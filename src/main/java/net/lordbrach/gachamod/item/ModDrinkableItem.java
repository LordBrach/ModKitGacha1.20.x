/*package net.lordbrach.gachamod.item;
import net.minecraft.world.item.Item;
import net.minecraft.world.item.ItemStack;
import org.jetbrains.annotations.NotNull;
import net.minecraft.world.item.UseAnim;
import net.minecraft.world.food.Foods;

public class ModDrinkableItem extends Item {
    public ModDrinkableItem(Foods food, Properties properties) {
        super(new Item.Properties().food(properties).group(GachaMod.GACHA_TAB));
    }
    public static final ModDrinkableItem ROOTBEER_ITEM = new ModDrinkableItem(new Item.Properties().food(ModFoods.ROOTBEER_FOOD));

    @Override
    public int getUseDuration(@NotNull ItemStack stack) {
        return 32;  // You can adjust the duration as needed
    }

    @Override
    public @NotNull UseAnim getUseAnimation(@NotNull ItemStack stack) {
        return UseAnim.DRINK;
    }
}
*/
